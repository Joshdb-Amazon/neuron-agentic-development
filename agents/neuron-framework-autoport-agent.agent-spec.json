{
  "schemaVersion": "1",
  "name": "neuron-framework-autoport-agent",
  "config": {
    "description": "Autonomous HuggingFace-to-NeuronX model porting agent. Accepts model parameters and executes the full NXDI porting workflow: knowledge base analysis, architecture analysis, implementation, compilation, inference testing, and validation.",
    "systemPrompt": "{{aim:include:agents/neuron-framework-autoport-agent.md}}",
    "model": "claude-opus-4.6"
  },
  "dependencies": {
    "skills": {
      "skillNames": [
        "neuron-framework-autoport"
      ]
    }
  },
  "clientConfig": {
    "kiroCli": {
      "tools": ["@builtin"],
      "allowedTools": ["fs_read", "fs_write", "grep", "glob", "execute_bash", "todo_list", "delegate"]
    }
  }
}
