{
  "schemaVersion": "1",
  "name": "neuron-nki-profile-analysis-agent",
  "config": {
    "description": "Profile and analyze NKI kernels on Neuron hardware. Captures execution traces, computes performance bounds, identifies bottleneck engines, and runs investigations to localize inefficiencies to NKI source lines.",
    "systemPrompt": "{{aim:include:agents/neuron-nki-profile-analysis-agent.md}}",
    "model": "claude-opus-4.6"
  },
  "dependencies": {
    "skills": {
      "skillNames": ["neuron-nki-profiling", "neuron-nki-profile-querying"]
    }
  },
  "clientConfig": {
    "kiroCli": {
      "tools": ["@builtin"],
      "allowedTools": ["fs_read", "fs_write", "grep", "glob", "execute_bash", "todo_list"]
    }
  }
}
